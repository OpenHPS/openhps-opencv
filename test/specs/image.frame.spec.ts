import 'mocha';
import { ImageFrame } from '../../src';

describe('image', () => {
    describe('frame', () => {

        it('should be serializable', () => {
            const frame = new ImageFrame();
        });
        
    });
});